# نظام إدارة مساعدات لجنة زكاة العامرات

نظام ويب متكامل لإدارة وتوزيع المساعدات الخيرية، مصمم باللغة العربية بالكامل (RTL) مع واجهة احترافية وسهلة الاستخدام.

---

## 📋 جدول المحتويات

1. [نظرة عامة على النظام](#-نظرة-عامة-على-النظام)
2. [المميزات الرئيسية](#-المميزات-الرئيسية)
3. [التقنيات المستخدمة](#-التقنيات-المستخدمة)
4. [فهم قاعدة البيانات - Supabase](#-فهم-قاعدة-البيانات---supabase)
5. [المتطلبات الأساسية](#-المتطلبات-الأساسية)
6. [إعداد المشروع - دليل شامل خطوة بخطوة](#-إعداد-المشروع---دليل-شامل-خطوة-بخطوة)
7. [هيكل المشروع](#-هيكل-المشروع)
8. [شرح قاعدة البيانات التفصيلي](#-شرح-قاعدة-البيانات-التفصيلي)
9. [إضافة بيانات تجريبية](#-إضافة-بيانات-تجريبية)
10. [دليل الاستخدام الكامل](#-دليل-الاستخدام-الكامل)
11. [رفع المشروع للإنتاج](#-رفع-المشروع-للإنتاج)
12. [حل المشاكل الشائعة](#-حل-المشاكل-الشائعة)

---

## 🎯 نظرة عامة على النظام

هذا نظام إدارة متكامل لتوزيع المساعدات الخيرية على الأسر المحتاجة. تم بناؤه بتقنيات حديثة لضمان:
- سهولة الاستخدام
- الأمان والموثوقية
- تتبع دقيق لجميع العمليات
- إدارة فعالة للموارد

**النظام يتكون من:**
- واجهة ويب بالكامل (لا يحتاج تطبيق)
- قاعدة بيانات سحابية (Supabase)
- نظام صلاحيات متعدد المستويات
- تقارير وإحصائيات شاملة

---

## ✨ المميزات الرئيسية

### 1. نظام صلاحيات متعدد المستويات

#### **مدير النظام (Admin)**
- إدارة كاملة للمستخدمين (إضافة، تعديل، تعطيل، حذف)
- إدارة المناطق الجغرافية
- عرض جميع التقارير والإحصائيات
- الوصول إلى سجل العمليات الكامل
- مراقبة أداء الموظفين والمديرين

#### **مدير اللجنة (Manager)**
- إدارة الأسر المستفيدة (إضافة، تعديل، بحث)
- إدارة أنواع المساعدات المتاحة
- إدارة المخزون (إضافة، تخصيص للموظفين)
- إنشاء وتوزيع المهام على الموظفين
- متابعة التسليمات وحالتها

#### **الموظف (Employee)**
- عرض المهام المكلف بها
- عرض التسليمات المسندة له
- تحديث حالة التسليمات (في الطريق، تم التسليم، لم يتم)
- عرض بيانات الأسر والمواقع
- إضافة ملاحظات على التسليمات

### 2. إدارة الأسر المستفيدة

**المعلومات المسجلة:**
- اسم العائلة ورب الأسرة
- رقم الهاتف للتواصل
- المنطقة الجغرافية
- العنوان التفصيلي
- رابط موقع Google Maps للوصول السهل
- عدد أفراد الأسرة
- ملاحظات خاصة بالأسرة

**المميزات:**
- بحث سريع بالاسم أو الهاتف
- فلترة حسب المنطقة
- تاريخ كامل للمساعدات المقدمة لكل أسرة
- تتبع آخر تحديث

### 3. إدارة المساعدات

**أنواع المساعدات المدعومة:**
- **غذائية**: أرز، سلة غذائية، لحوم، مواد تموينية
- **مالية**: نقد، قسائم شراء، مساعدات طارئة
- **طبية**: أدوية، فواتير علاج، مستلزمات طبية
- **تعليمية**: رسوم دراسية، قرطاسية، كتب
- **أخرى**: حسب الحاجة

**لكل مساعدة:**
- اسم واضح
- نوع المساعدة
- وصف تفصيلي
- تاريخ الإنشاء
- من أنشأها

### 4. نظام المخزون المتطور

**إدارة شاملة للمخزون:**
- إضافة عناصر جديدة للمخزون
- تحديد الكميات المتوفرة
- تخصيص عناصر لموظفين محددين
- تتبع حالة كل عنصر (متوفر، مخصص، موزع)
- إضافة ملاحظات على كل عنصر

**التحديث التلقائي:**
- عند تسليم مساعدة، تُخصم الكمية تلقائياً
- تنبيهات عند انخفاض المخزون
- سجل كامل لحركات المخزون

### 5. نظام التسليمات

**إدارة التسليمات:**
- جدولة التسليمات للأسر
- إسناد التسليمات للموظفين
- تحديد الكميات لكل تسليم
- تحديد تاريخ التسليم المجدول

**حالات التسليم:**
- **في الطريق**: الموظف في طريقه للأسرة
- **تم التسليم**: تم التسليم بنجاح
- **لم يتم**: لم يتم التسليم (مع ذكر السبب)

**الميزات الإضافية:**
- تسجيل تاريخ ووقت التسليم الفعلي
- إضافة ملاحظات على التسليم
- خصم تلقائي من المخزون عند التسليم
- تسجيل العملية في Audit Log

### 6. نظام المهام

**إنشاء وإدارة المهام:**
- عنوان ووصف واضح للمهمة
- تحديد الموظف المكلف
- تحديد مستوى الأولوية (عالية، متوسطة، منخفضة)
- تحديد تاريخ الاستحقاق

**تتبع المهام:**
- حالة المهمة (جديدة، قيد التنفيذ، مكتملة)
- تاريخ الإنشاء ومن أنشأها
- تاريخ الإنجاز
- إشعارات للموظفين

### 7. سجل العمليات (Operation Logs)

**تسجيل شامل:**
- كل عملية تحدث في النظام تُسجل
- معلومات مسجلة:
  - من قام بالعملية
  - تاريخ ووقت العملية
  - نوع العملية (إضافة، تعديل، حذف)
  - الجدول المتأثر
  - تفاصيل إضافية (JSONB format)

**الفائدة:**
- مراجعة وتدقيق كامل
- معرفة من غيّر ماذا ومتى
- الشفافية والمساءلة

---

## 🛠 التقنيات المستخدمة

### Frontend (الواجهة الأمامية)

#### **React 18**
- مكتبة JavaScript لبناء واجهات مستخدم تفاعلية
- نظام Components لإعادة استخدام الكود
- تحديثات سريعة وفعالة

#### **TypeScript**
- لغة برمجة مبنية على JavaScript
- تضيف نظام الأنواع (types) للأمان
- تقلل الأخطاء وتحسن الكود

#### **Vite**
- أداة بناء حديثة وسريعة جداً
- Hot Module Replacement (HMR) فوري
- بناء سريع للإنتاج

#### **Tailwind CSS**
- إطار عمل CSS utility-first
- دعم كامل لـ RTL (اللغة العربية)
- تصميم responsive لجميع الأجهزة

#### **Lucide React**
- مكتبة أيقونات حديثة وجميلة
- أكثر من 1000 أيقونة
- سهلة الاستخدام مع React

### Backend & Database (الخلفية وقاعدة البيانات)

#### **Supabase** - الحل الشامل
Supabase هي منصة مفتوحة المصدر توفر:

1. **قاعدة بيانات PostgreSQL**
   - قاعدة بيانات علائقية قوية
   - تدعم JSON و JSONB
   - تدعم العلاقات المعقدة
   - سريعة وموثوقة

2. **Authentication (المصادقة)**
   - نظام تسجيل دخول جاهز
   - إدارة المستخدمين
   - JWT tokens آمنة

3. **Row Level Security (RLS)**
   - حماية على مستوى الصفوف
   - كل مستخدم يرى بياناته فقط
   - قواعد أمان قوية

4. **Edge Functions**
   - وظائف serverless
   - تعمل في السحابة
   - سريعة وقابلة للتوسع

5. **Realtime Subscriptions**
   - تحديثات فورية
   - بدون إعادة تحميل الصفحة

### الأمان

- **Row Level Security (RLS)**: كل جدول محمي
- **JWT Authentication**: توكنات آمنة
- **HTTPS**: تشفير كامل
- **Environment Variables**: لا معلومات حساسة في الكود

---

## 🗄️ فهم قاعدة البيانات - Supabase

### ما هي Supabase؟

Supabase هي **بديل مفتوح المصدر لـ Firebase**. توفر قاعدة بيانات PostgreSQL في السحابة مع:
- لوحة تحكم سهلة
- أدوات مطورين قوية
- أمان عالي
- خطة مجانية سخية

### لماذا Supabase؟

1. **مجانية للبداية**
   - قاعدة بيانات PostgreSQL مجانية
   - 500 MB تخزين
   - 2 GB نقل بيانات شهرياً
   - كافية لآلاف المستخدمين

2. **سهلة الاستخدام**
   - لوحة تحكم بصرية
   - SQL Editor مدمج
   - أدوات إدارة جاهزة

3. **قوية ومرنة**
   - PostgreSQL كامل
   - دعم للعلاقات المعقدة
   - إمكانيات متقدمة

4. **آمنة**
   - Row Level Security
   - تشفير كامل
   - نسخ احتياطي تلقائي

### أين موجودة قاعدة البيانات؟

**في السحابة (Cloud)!**
- قاعدة البيانات موجودة على سيرفرات Supabase
- يمكنك الوصول إليها من أي مكان
- **لا تحتاج لتثبيت شيء على جهازك**
- **لا تحتاج لإدارة سيرفر**

### كيف تتصل التطبيق بقاعدة البيانات؟

```
[متصفح المستخدم]
       ↓
[تطبيق React على Vercel/Netlify]
       ↓
[Supabase Client (مكتبة JavaScript)]
       ↓
[Supabase API]
       ↓
[قاعدة بيانات PostgreSQL في السحابة]
```

**ملف `.env` يحتوي على معلومات الاتصال:**
```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### ما الذي تحتاج لنقله أو رفعه؟

**فقط كود الواجهة الأمامية (Frontend)!**

✅ **ما يُرفع:**
- ملفات HTML, CSS, JavaScript المبنية (مجلد `dist`)
- رفعها على Vercel/Netlify/VPS

❌ **ما لا يُرفع:**
- قاعدة البيانات (موجودة على Supabase)
- سيرفر (Supabase توفره)
- نظام المصادقة (Supabase توفره)

**فقط تأكد من:**
- متغيرات البيئة صحيحة على خدمة الاستضافة
- مشروع Supabase مفعّل

---

## 📦 المتطلبات الأساسية

### 1. Node.js و npm

**Node.js** هو بيئة تشغيل JavaScript على جهازك.

**التثبيت:**
1. اذهب إلى https://nodejs.org/
2. حمّل النسخة LTS (الموصى بها)
3. ثبّت البرنامج
4. افتح Terminal وتحقق:
   ```bash
   node --version
   # يجب أن ترى: v18.x.x أو أحدث

   npm --version
   # يجب أن ترى: 9.x.x أو أحدث
   ```

### 2. محرر نصوص (Code Editor)

**موصى به: Visual Studio Code**
- مجاني ومفتوح المصدر
- تحميل من: https://code.visualstudio.com/
- extensions مفيدة:
  - ESLint
  - Prettier
  - Tailwind CSS IntelliSense

### 3. Git (اختياري)

لإدارة النسخ والتحديثات:
- تحميل من: https://git-scm.com/

### 4. حساب Supabase (مجاني)

- اذهب إلى: https://supabase.com/
- أنشئ حساب (مجاناً)
- لا تحتاج بطاقة ائتمان

---

## 🚀 إعداد المشروع - دليل شامل خطوة بخطوة

### المرحلة الأولى: إعداد قاعدة البيانات (Supabase)

#### الخطوة 1: إنشاء مشروع Supabase

1. **اذهب إلى https://supabase.com/**
2. **اضغط "Start your project"**
3. **سجل دخول:**
   - يمكنك استخدام GitHub
   - أو بريد إلكتروني وكلمة مرور

4. **اضغط "New Project"**
5. **املأ المعلومات:**
   - **Name**: `zakat-amerat` (أو أي اسم)
   - **Database Password**: اختر كلمة مرور قوية
     - **مهم جداً: احفظ هذه الكلمة في مكان آمن!**
     - مثال: `MyStr0ng#Pass2024!`
   - **Region**: اختر أقرب منطقة لك
     - للخليج: `Southeast Asia (Singapore)`
     - للمغرب العربي: `West EU (Ireland)`
   - **Pricing Plan**: Free (مجاني)

6. **اضغط "Create new project"**
7. **انتظر 1-2 دقيقة** حتى يكتمل إنشاء المشروع

#### الخطوة 2: الحصول على معلومات الاتصال

بعد إنشاء المشروع:

1. **اذهب إلى "Settings" في القائمة الجانبية**
2. **اضغط على "API"**
3. **ستجد معلومتين مهمتين:**

   **أ) Project URL:**
   ```
   https://gboyxufgbiqctpgfmwki.supabase.co
   ```

   **ب) anon/public key:**
   ```
   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6...
   ```

4. **انسخ هذين القيمتين في ملف نصي** - ستحتاجهما لاحقاً!

#### الخطوة 3: إنشاء الجداول (Database Schema)

الآن نحتاج لإنشاء جداول قاعدة البيانات:

1. **في لوحة Supabase، اضغط على "SQL Editor"** من القائمة الجانبية

2. **اضغط "New query"**

3. **افتح ملف المشروع:**
   - في مجلد المشروع، اذهب إلى:
   - `supabase/migrations/20251218052044_create_complete_schema.sql`

4. **انسخ كل محتوى الملف**

5. **الصقه في SQL Editor في Supabase**

6. **اضغط "Run" أو `Ctrl+Enter`**

7. **يجب أن ترى:**
   ```
   Success. No rows returned
   ```

8. **تحقق من الجداول:**
   - اضغط على "Table Editor" في القائمة الجانبية
   - يجب أن ترى الجداول التالية:
     - profiles
     - regions
     - families
     - assistances
     - inventory
     - deliveries
     - tasks
     - operation_logs

✅ **قاعدة البيانات جاهزة!**

#### الخطوة 4: إنشاء أول مستخدم (Admin)

الآن نحتاج مستخدم للدخول للنظام:

**الطريقة السهلة - استخدام Sample Data:**

1. **في SQL Editor، اضغط "New query"**

2. **افتح ملف `sample-data.sql` من مجلد المشروع**

3. **انسخ كل المحتوى والصقه في SQL Editor**

4. **اضغط "Run"**

5. **سيتم إنشاء:**
   - **3 مستخدمين:**
     - Admin: `admin@zakat.com` / `Admin@123`
     - Manager: `manager@zakat.com` / `Manager@123`
     - Employee: `employee@zakat.com` / `Employee@123`
   - 3 مناطق
   - 10 أسر
   - عدة مساعدات ومخزون

✅ **البيانات التجريبية جاهزة!**

**الطريقة اليدوية (إذا أردت إنشاء مستخدم واحد فقط):**

1. **في Supabase Dashboard:**
   - اذهب إلى "Authentication" > "Users"
   - اضغط "Add user" > "Create new user"
   - Email: `admin@zakat.com`
   - Password: `Admin@123`
   - ✅ Auto Confirm User (مهم!)
   - اضغط "Create user"
   - **انسخ User ID** (ستحتاجه)

2. **أضف بيانات في جدول profiles:**
   - اذهب إلى "SQL Editor"
   - نفذ هذا الأمر (استبدل USER_ID):

   ```sql
   INSERT INTO profiles (id, full_name, role, phone, is_active)
   VALUES (
     'USER_ID_هنا',
     'مدير النظام',
     'admin',
     '99999999',
     true
   );
   ```

---

### المرحلة الثانية: إعداد الكود على جهازك

#### الخطوة 5: تحميل الكود

**إذا كان لديك Git:**
```bash
git clone <repository-url>
cd <project-folder>
```

**أو:**
- حمّل ملف ZIP
- فك الضغط
- افتح المجلد في Terminal

#### الخطوة 6: تثبيت المكتبات

في Terminal، في مجلد المشروع:

```bash
npm install
```

**ما يحدث:**
- npm يقرأ ملف `package.json`
- يحمّل جميع المكتبات المطلوبة
- يحفظها في مجلد `node_modules`
- قد يأخذ 1-3 دقائق حسب سرعة الإنترنت

#### الخطوة 7: إنشاء ملف .env

**هذا الملف يحتوي على معلومات الاتصال بـ Supabase**

1. **في المجلد الرئيسي للمشروع، أنشئ ملف جديد اسمه `.env`**

2. **أضف المحتوى التالي:**

```env
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key
```

3. **استبدل القيم:**
   - `your-project-url`: ضع Project URL من Supabase
   - `your-anon-key`: ضع anon key من Supabase

**مثال على ملف .env الصحيح:**

```env
VITE_SUPABASE_URL=https://gboyxufgbiqctpgfmwki.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imdib3l4dWZnYmlxY3RwZ2Ztd2tpIiwicm9sZSI6ImFub24iLCJpYXQiOjE2ODk5NjE4MzQsImV4cCI6MjAwNTUzNzgzNH0.ABC123...
```

**⚠️ مهم جداً:**
- **لا تشارك هذا الملف مع أحد**
- **لا ترفعه على GitHub**
- **تأكد من وجوده في `.gitignore`**

#### الخطوة 8: تشغيل المشروع

```bash
npm run dev
```

**ما يحدث:**
- Vite يبدأ Development Server
- يفتح على المنفذ 5173
- يراقب التغييرات تلقائياً

**ستر ى رسالة:**
```
  VITE v5.4.8  ready in 324 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

#### الخطوة 9: فتح المتصفح

1. **افتح المتصفح (Chrome, Firefox, Edge, Safari)**
2. **اذهب إلى: `http://localhost:5173`**
3. **يجب أن ترى صفحة تسجيل الدخول!**

#### الخطوة 10: تسجيل الدخول

**إذا استخدمت sample-data.sql:**
- البريد: `admin@zakat.com`
- كلمة المرور: `Admin@123`

**اضغط "تسجيل الدخول"**

**يجب أن تنتقل إلى لوحة التحكم!**

🎉 **تهانينا! النظام يعمل!**

---

## 📁 هيكل المشروع

```
project/
├── src/                              # الكود المصدري
│   ├── components/                   # مكونات React
│   │   ├── admin/                   # مكونات المدير
│   │   │   ├── UsersManager.tsx     # إدارة المستخدمين
│   │   │   ├── RegionsManager.tsx   # إدارة المناطق
│   │   │   ├── Reports.tsx          # التقارير
│   │   │   └── OperationLogs.tsx    # سجل العمليات
│   │   ├── manager/                 # مكونات مدير اللجنة
│   │   │   ├── FamiliesManager.tsx  # إدارة الأسر
│   │   │   ├── AssistancesManager.tsx # إدارة المساعدات
│   │   │   ├── InventoryManager.tsx # إدارة المخزون
│   │   │   └── TasksManager.tsx     # إدارة المهام
│   │   ├── employee/                # مكونات الموظف
│   │   │   └── DeliveriesManager.tsx # إدارة التسليمات
│   │   ├── dashboards/              # لوحات التحكم
│   │   │   ├── AdminDashboard.tsx   # لوحة المدير
│   │   │   ├── ManagerDashboard.tsx # لوحة مدير اللجنة
│   │   │   └── EmployeeDashboard.tsx # لوحة الموظف
│   │   ├── Dashboard.tsx            # توجيه اللوحات
│   │   ├── Layout.tsx               # تخطيط عام
│   │   └── Login.tsx                # صفحة تسجيل الدخول
│   ├── contexts/                    # React Contexts
│   │   └── AuthContext.tsx          # سياق المصادقة
│   ├── lib/                         # مكتبات ومساعدات
│   │   └── supabase.ts              # إعداد Supabase
│   ├── App.tsx                      # المكون الرئيسي
│   ├── main.tsx                     # نقطة البداية
│   └── index.css                    # الأنماط الرئيسية
│
├── supabase/                         # ملفات Supabase
│   ├── migrations/                  # تهجيرات قاعدة البيانات
│   │   ├── 20251218045327_fix_profiles_policies.sql
│   │   ├── 20251218050609_complete_fix_profiles_policies.sql
│   │   └── 20251218052044_create_complete_schema.sql
│   └── functions/                   # Edge Functions
│       └── create-user/
│           └── index.ts             # وظيفة إنشاء مستخدمين
│
├── public/                           # ملفات ثابتة (صور، أيقونات)
├── .env                             # متغيرات البيئة (سري!)
├── .env.example                     # مثال لملف البيئة
├── .gitignore                       # ملفات يتجاهلها Git
├── package.json                     # معلومات المشروع
├── package-lock.json                # نسخ المكتبات المثبتة
├── tsconfig.json                    # إعدادات TypeScript
├── tsconfig.app.json                # إعدادات TypeScript للتطبيق
├── tsconfig.node.json               # إعدادات TypeScript لـ Node
├── vite.config.ts                   # إعدادات Vite
├── tailwind.config.js               # إعدادات Tailwind CSS
├── postcss.config.js                # إعدادات PostCSS
├── eslint.config.js                 # إعدادات ESLint
├── sample-data.sql                  # بيانات تجريبية
├── SETUP.md                         # دليل الإعداد
└── README.md                        # هذا الملف
```

### شرح الملفات المهمة

#### `src/lib/supabase.ts`
**دور الملف:** إنشاء اتصال مع Supabase

```typescript
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY

export const supabase = createClient(supabaseUrl, supabaseAnonKey)
```

- يقرأ المعلومات من `.env`
- ينشئ client واحدة للكل التطبيق
- تُستخدم في جميع الصفحات

#### `src/contexts/AuthContext.tsx`
**دور الملف:** إدارة حالة تسجيل الدخول

**الوظائف:**
- `signIn()`: تسجيل الدخول
- `signOut()`: تسجيل الخروج
- `user`: معلومات المستخدم الحالي
- `userProfile`: بيانات المستخدم من جدول profiles

#### `src/components/Login.tsx`
صفحة تسجيل الدخول - أول ما يراه المستخدم

#### `src/components/Dashboard.tsx`
يفحص دور المستخدم ويعرض اللوحة المناسبة

#### `.env`
ملف **سري** يحتوي على:
```env
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...
```

**⚠️ لا تشاركه أبداً!**

---

## 🗄️ شرح قاعدة البيانات التفصيلي

### الجداول الثمانية

#### 1. جدول `profiles`
**الغرض:** معلومات المستخدمين وصلاحياتهم

**الأعمدة:**
```sql
id          uuid          # نفس ID من auth.users
full_name   text          # الاسم الكامل
role        text          # الدور: admin, manager, employee
phone       text          # رقم الهاتف
is_active   boolean       # هل الحساب نشط؟
created_at  timestamptz   # تاريخ الإنشاء
updated_at  timestamptz   # آخر تحديث
```

**الأدوار:**
- `admin`: مدير النظام
- `manager`: مدير اللجنة
- `employee`: موظف

**Policies (RLS):**
- الكل يمكنه قراءة profiles
- Admin فقط يمكنه الإضافة/التعديل/الحذف

#### 2. جدول `regions`
**الغرض:** المناطق الجغرافية

**الأعمدة:**
```sql
id           uuid          # معرف فريد
name         text          # اسم المنطقة
description  text          # وصف المنطقة
created_at   timestamptz   # تاريخ الإنشاء
created_by   uuid          # من أنشأها (FK → profiles)
```

**مثال:**
| id | name | description |
|----|------|-------------|
| xxx | العامرات الأولى | المنطقة الأولى |
| yyy | العامرات الثانية | المنطقة الثانية |

**Policies:**
- الكل يمكنه القراءة
- Admin فقط يمكنه الإضافة/التعديل/الحذف

#### 3. جدول `families`
**الغرض:** بيانات الأسر المستفيدة

**الأعمدة:**
```sql
id             uuid          # معرف فريد
family_name    text          # اسم العائلة
head_of_family text          # رب الأسرة
phone          text          # رقم الهاتف
region_id      uuid          # المنطقة (FK → regions)
address        text          # العنوان
location_link  text          # رابط Google Maps
family_size    integer       # عدد الأفراد
notes          text          # ملاحظات
created_at     timestamptz   # تاريخ الإنشاء
created_by     uuid          # من أنشأها (FK → profiles)
updated_at     timestamptz   # آخر تحديث
```

**مثال:**
```
اسم العائلة: عائلة الأحمد
رب الأسرة: أحمد محمد
الهاتف: 99123456
المنطقة: العامرات الأولى
العنوان: شارع 5، منزل 23
الموقع: https://maps.google.com/...
عدد الأفراد: 6
ملاحظات: الأب مريض
```

**Policies:**
- Admin و Manager يمكنهم القراءة
- Manager يمكنه الإضافة/التعديل
- Admin يمكنه الحذف

#### 4. جدول `assistances`
**الغرض:** أنواع المساعدات المتاحة

**الأعمدة:**
```sql
id          uuid          # معرف فريد
name        text          # اسم المساعدة
type        text          # نوع المساعدة
description text          # وصف
created_at  timestamptz   # تاريخ الإنشاء
created_by  uuid          # من أنشأها (FK → profiles)
```

**أنواع المساعدات:**
- غذائية
- مالية
- طبية
- تعليمية
- أخرى

**أمثلة:**
| name | type | description |
|------|------|-------------|
| سلة غذائية | غذائية | سلة غذائية شاملة |
| أرز 20 كيس | غذائية | كيس أرز 25 كجم |
| قسيمة 100 ريال | مالية | قسيمة شراء |

**Policies:**
- الكل يمكنه القراءة
- Manager يمكنه الإضافة/التعديل
- Admin يمكنه الحذف

#### 5. جدول `inventory`
**الغرض:** المخزون المتاح من المساعدات

**الأعمدة:**
```sql
id            uuid          # معرف فريد
assistance_id uuid          # نوع المساعدة (FK → assistances)
quantity      integer       # الكمية المتوفرة
assigned_to   uuid          # مخصصة لموظف (FK → profiles, nullable)
status        text          # الحالة: متوفر، مخصص، موزع
notes         text          # ملاحظات
created_at    timestamptz   # تاريخ الإنشاء
updated_at    timestamptz   # آخر تحديث
```

**حالات المخزون:**
- `متوفر`: متاح للتوزيع
- `مخصص`: تم تخصيصه لموظف
- `موزع`: تم توزيعه بالكامل

**مثال:**
```
نوع المساعدة: سلة غذائية
الكمية: 50
مخصصة لـ: محمد (موظف)
الحالة: مخصص
ملاحظات: للمنطقة الأولى
```

**التحديث التلقائي:**
- عند تسليم مساعدة، تُخصم الكمية تلقائياً

**Policies:**
- الكل يمكنه القراءة
- Manager يمكنه الإضافة/التعديل
- Admin يمكنه الحذف

#### 6. جدول `deliveries`
**الغرض:** التسليمات للأسر

**الأعمدة:**
```sql
id             uuid          # معرف فريد
family_id      uuid          # العائلة (FK → families)
employee_id    uuid          # الموظف المسؤول (FK → profiles)
inventory_id   uuid          # عنصر المخزون (FK → inventory)
quantity       integer       # الكمية
status         text          # حالة التسليم
delivery_notes text          # ملاحظات
scheduled_date date          # تاريخ التسليم المجدول
delivered_at   timestamptz   # تاريخ التسليم الفعلي
created_at     timestamptz   # تاريخ الإنشاء
```

**حالات التسليم:**
- `في الطريق`: الموظف في طريقه
- `تم التسليم`: تم بنجاح
- `لم يتم`: لم يتم (مع السبب في الملاحظات)

**عند تحديث الحالة إلى "تم التسليم":**
1. يتم تسجيل `delivered_at` تلقائياً
2. يتم خصم الكمية من `inventory`
3. يتم تسجيل العملية في `operation_logs`

**مثال:**
```
العائلة: عائلة الأحمد
الموظف: محمد أحمد
نوع المساعدة: سلة غذائية
الكمية: 2
الحالة: تم التسليم
التاريخ المجدول: 2024-12-18
التاريخ الفعلي: 2024-12-18 14:30
ملاحظات: استلم رب الأسرة
```

**Policies:**
- Employee يرى تسليماته فقط
- Manager و Admin يرون الكل
- Employee يمكنه تحديث حالة تسليماته
- Manager يمكنه إنشاء تسليمات جديدة

#### 7. جدول `tasks`
**الغرض:** المهام الموزعة على الموظفين

**الأعمدة:**
```sql
id          uuid          # معرف فريد
title       text          # عنوان المهمة
description text          # وصف المهمة
assigned_to uuid          # الموظف المكلف (FK → profiles)
status      text          # حالة المهمة
priority    text          # الأولوية
due_date    date          # تاريخ الاستحقاق
created_at  timestamptz   # تاريخ الإنشاء
created_by  uuid          # من أنشأها (FK → profiles)
completed_at timestamptz  # تاريخ الإنجاز
```

**حالات المهمة:**
- `جديدة`: لم تُنفذ بعد
- `قيد التنفيذ`: يعمل عليها
- `مكتملة`: تم إنجازها

**مستويات الأولوية:**
- `عالية`: مستعجلة
- `متوسطة`: عادية
- `منخفضة`: يمكن تأجيلها

**مثال:**
```
العنوان: توزيع مساعدات منطقة العامرات الأولى
الوصف: توزيع 10 سلال غذائية على الأسر المستفيدة
الموظف: محمد أحمد
الأولوية: عالية
الاستحقاق: 2024-12-20
الحالة: قيد التنفيذ
```

**Policies:**
- Employee يرى مهامه فقط
- Manager يرى جميع المهام
- Manager يمكنه إنشاء وتعديل المهام

#### 8. جدول `operation_logs`
**الغرض:** سجل جميع العمليات (Audit Log)

**الأعمدة:**
```sql
id         uuid          # معرف فريد
user_id    uuid          # من قام بالعملية (FK → profiles)
action     text          # نوع العملية
table_name text          # الجدول المتأثر
record_id  uuid          # ID السجل المتأثر
details    jsonb         # تفاصيل إضافية
created_at timestamptz   # تاريخ ووقت العملية
```

**أنواع العمليات:**
- إضافة مستخدم
- تعديل أسرة
- حذف منطقة
- تسليم مساعدة
- إنشاء مهمة
- تحديث مخزون

**مثال على سجل:**
```json
{
  "user_id": "xxx-xxx-xxx",
  "action": "إضافة أسرة جديدة",
  "table_name": "families",
  "record_id": "yyy-yyy-yyy",
  "details": {
    "family_name": "عائلة الأحمد",
    "region": "العامرات الأولى",
    "family_size": 6
  },
  "created_at": "2024-12-18T10:30:00Z"
}
```

**الفائدة:**
- المساءلة والشفافية
- معرفة من غيّر ماذا ومتى
- مراجعة وتدقيق

**Policies:**
- Admin فقط يمكنه القراءة
- النظام يضيف سجلات تلقائياً

---

## 📊 إضافة بيانات تجريبية

### استخدام ملف sample-data.sql

الملف `sample-data.sql` يحتوي على بيانات تجريبية جاهزة:

**الخطوات:**

1. **افتح Supabase Dashboard**
2. **اذهب إلى SQL Editor**
3. **اضغط "New query"**
4. **افتح ملف `sample-data.sql`**
5. **انسخ كل المحتوى**
6. **الصقه في Editor**
7. **اضغط "Run"**

**ما سيتم إنشاؤه:**

#### مستخدمين (3):
1. **Admin**
   - البريد: `admin@zakat.com`
   - كلمة المرور: `Admin@123`
   - الصلاحيات: كاملة

2. **Manager**
   - البريد: `manager@zakat.com`
   - كلمة المرور: `Manager@123`
   - الصلاحيات: إدارة الأسر والمساعدات

3. **Employee**
   - البريد: `employee@zakat.com`
   - كلمة المرور: `Employee@123`
   - الصلاحيات: تنفيذ المهام

#### مناطق (3):
- العامرات الأولى
- العامرات الثانية
- العامرات الثالثة

#### أسر (10):
أسر موزعة على المناطق الثلاث بمعلومات كاملة

#### مساعدات (5):
- سلة غذائية
- أرز 20 كيس
- لحم 10 كيلو
- قسيمة شراء 100 ريال
- دفع فاتورة كهرباء

#### مخزون:
- 50 سلة غذائية
- 100 كيس أرز
- 30 كيلو لحم

#### تسليمات:
عدة تسليمات بحالات مختلفة للتجربة

#### مهام:
مهام موزعة على الموظف

---

## 📖 دليل الاستخدام الكامل

### للمدير (Admin)

#### 1. تسجيل الدخول
- افتح http://localhost:5173
- البريد: `admin@zakat.com`
- كلمة المرور: `Admin@123`
- اضغط "تسجيل الدخول"

#### 2. إدارة المستخدمين

**إضافة مستخدم جديد:**
1. من لوحة المدير، اضغط "إدارة المستخدمين"
2. اضغط زر "إضافة مستخدم" (أيقونة +)
3. املأ النموذج:
   - البريد الإلكتروني
   - كلمة المرور (8 أحرف على الأقل)
   - الاسم الكامل
   - رقم الهاتف
   - الدور (اختر من القائمة)
4. اضغط "إضافة"
5. سيظهر المستخدم في القائمة فوراً

**تعديل مستخدم:**
1. اضغط على أيقونة القلم بجانب المستخدم
2. عدّل المعلومات المطلوبة
3. اضغط "حفظ"

**تعطيل/تفعيل مستخدم:**
1. استخدم الزر "تعطيل" / "تفعيل"
2. المستخدم المعطّل لا يستطيع تسجيل الدخول

**حذف مستخدم:**
1. اضغط على أيقونة سلة المهملات
2. أكد الحذف
3. **تحذير:** الحذف نهائي!

#### 3. إدارة المناطق

**إضافة منطقة:**
1. اضغط "إدارة المناطق"
2. اضغط "إضافة منطقة"
3. أدخل:
   - اسم المنطقة
   - وصف المنطقة (اختياري)
4. احفظ

**تعديل/حذف:**
- نفس طريقة المستخدمين

#### 4. عرض التقارير

اضغط "التقارير" لعرض:
- إجمالي الأسر
- إجمالي المساعدات
- التسليمات المكتملة
- المهام المعلقة
- الموظفين النشطين
- إحصائيات أخرى

#### 5. سجل العمليات

**عرض جميع العمليات:**
1. اضغط "سجل العمليات"
2. شاهد قائمة بجميع العمليات
3. لكل عملية:
   - من قام بها
   - متى (تاريخ ووقت دقيق)
   - نوع العملية
   - الجدول المتأثر
   - التفاصيل (JSON)

**البحث والفلترة:**
- ابحث بالمستخدم
- فلتر حسب التاريخ
- فلتر حسب نوع العملية

---

### لمدير اللجنة (Manager)

#### 1. تسجيل الدخول
- البريد: `manager@zakat.com`
- كلمة المرور: `Manager@123`

#### 2. إدارة الأسر

**إضافة أسرة جديدة:**
1. اضغط "إدارة الأسر"
2. اضغط "إضافة أسرة"
3. املأ النموذج بعناية:

   **معلومات أساسية:**
   - اسم العائلة (مثل: عائلة الأحمد)
   - اسم رب الأسرة (مثل: أحمد محمد)
   - رقم الهاتف (مثل: 99123456)

   **معلومات الموقع:**
   - المنطقة (اختر من القائمة)
   - العنوان التفصيلي (مثل: شارع 5، منزل 23)
   - رابط Google Maps (اختياري):
     - افتح Google Maps
     - ابحث عن العنوان
     - اضغط "مشاركة"
     - انسخ الرابط
     - الصقه هنا

   **معلومات إضافية:**
   - عدد أفراد الأسرة
   - ملاحظات (مثل: الأب مريض، أم عاملة، إلخ)

4. اضغط "حفظ"
5. ستظهر الأسرة في القائمة

**البحث عن أسرة:**
- استخدم مربع البحث
- ابحث بـ:
  - اسم العائلة
  - رقم الهاتف
  - اسم رب الأسرة

**فلترة حسب المنطقة:**
- اختر منطقة من القائمة المنسدلة
- ستظهر أسر تلك المنطقة فقط

**تعديل أسرة:**
1. اضغط أيقونة القلم
2. عدّل المعلومات
3. احفظ

**عرض تفاصيل أسرة:**
1. اضغط على اسم الأسرة
2. شاهد:
   - جميع المعلومات
   - تاريخ المساعدات المقدمة
   - التسليمات السابقة

#### 3. إدارة المساعدات

**إضافة نوع مساعدة جديد:**
1. اضغط "إدارة المساعدات"
2. اضغط "إضافة مساعدة"
3. أدخل:
   - اسم المساعدة (واضح ومحدد)
     - أمثلة جيدة: "سلة غذائية شاملة"، "أرز 25 كجم"
     - أمثلة سيئة: "مساعدة"، "شيء"
   - نوع المساعدة (اختر من القائمة)
   - وصف تفصيلي (محتويات، قيمة، إلخ)
4. احفظ

**التصنيفات المتاحة:**
- غذائية
- مالية
- طبية
- تعليمية
- أخرى

#### 4. إدارة المخزون

**إضافة عنصر للمخزون:**
1. اضغط "إدارة المخزون"
2. اضغط "إضافة للمخزون"
3. املأ:
   - نوع المساعدة (اختر من قائمة المساعدات الموجودة)
   - الكمية (رقم)
   - تخصيص لموظف (اختياري):
     - اختر موظف من القائمة
     - ستتغير الحالة إلى "مخصص"
   - الحالة:
     - متوفر: موجود ومتاح
     - مخصص: تم تخصيصه لموظف
     - موزع: تم توزيعه بالكامل
   - ملاحظات (اختياري)
4. احفظ

**تحديث الكمية:**
1. اضغط "تعديل" على العنصر
2. غيّر الكمية
3. احفظ

**تخصيص لموظف:**
1. اضغط "تعديل"
2. اختر الموظف من القائمة
3. احفظ
4. الموظف سيرى هذه المواد في حسابه

**عرض حالة المخزون:**
- القائمة تعرض:
  - نوع المساعدة
  - الكمية المتوفرة
  - الحالة
  - مخصصة لمن (إن وُجد)
  - آخر تحديث

#### 5. إدارة المهام

**إنشاء مهمة جديدة:**
1. اضغط "إدارة المهام"
2. اضغط "إنشاء مهمة"
3. املأ النموذج:

   **معلومات المهمة:**
   - عنوان المهمة (واضح ومختصر)
     - مثال جيد: "توزيع 10 سلال على منطقة العامرات الأولى"
     - مثال سيئ: "توزيع"

   - وصف المهمة (تفصيلي):
     - ما المطلوب؟
     - أين؟
     - متى؟
     - ملاحظات خاصة

   - الموظف المكلف:
     - اختر من قائمة الموظفين

   - الأولوية:
     - عالية: مستعجلة جداً
     - متوسطة: عادية
     - منخفضة: يمكن تأجيلها

   - تاريخ الاستحقاق:
     - متى يجب إنجازها؟

4. احفظ
5. الموظف سيرى المهمة في لوحته

**متابعة المهام:**
- عرض جميع المهام
- فلترة حسب:
  - الحالة (جديدة، قيد التنفيذ، مكتملة)
  - الموظف
  - الأولوية
- شاهد تقدم كل موظف

**تعديل مهمة:**
- يمكنك تعديل أي معلومة
- لكن لا يمكن تغيير الحالة إلى "مكتملة" إلا من الموظف

---

### للموظف (Employee)

#### 1. تسجيل الدخول
- البريد: `employee@zakat.com`
- كلمة المرور: `Employee@123`

#### 2. عرض المهام

**لوحة المهام:**
- ستظهر لك جميع المهام المكلف بها
- لكل مهمة ستجد:
  - العنوان
  - الوصف الكامل
  - الأولوية (بألوان مختلفة)
  - تاريخ الاستحقاق
  - الحالة الحالية

**تحديث حالة مهمة:**
1. اضغط على المهمة
2. غيّر الحالة:
   - جديدة → قيد التنفيذ (عند البدء)
   - قيد التنفيذ → مكتملة (عند الانتهاء)
3. احفظ

#### 3. إدارة التسليمات

**عرض التسليمات:**
1. اضغط "إدارة التسليمات"
2. ستظهر قائمة بجميع التسليمات المسندة لك
3. لكل تسليم:
   - اسم العائلة
   - المنطقة
   - العنوان التفصيلي
   - رابط الموقع (افتحه للوصول)
   - نوع المساعدة
   - الكمية
   - التاريخ المجدول
   - الحالة

**تحديث حالة تسليم:**

1. **قبل الذهاب:**
   - اضغط "تحديث الحالة"
   - اختر "في الطريق"
   - احفظ

2. **عند التسليم بنجاح:**
   - اضغط "تحديث الحالة"
   - اختر "تم التسليم"
   - أضف ملاحظات (مثل: "استلم رب الأسرة")
   - احفظ
   - **سيحدث تلقائياً:**
     - تسجيل تاريخ ووقت التسليم
     - خصم الكمية من المخزون
     - تسجيل العملية في Audit Log

3. **إذا لم يتم التسليم:**
   - اضغط "تحديث الحالة"
   - اختر "لم يتم"
   - **مهم:** أضف السبب في الملاحظات:
     - "العائلة غير موجودة"
     - "العنوان خاطئ"
     - "رفضوا الاستلام"
     - إلخ
   - احفظ

**استخدام رابط الموقع:**
- اضغط على رابط الموقع
- سيفتح Google Maps
- اضغط "اتجاهات"
- اتبع التوجيهات

**إضافة ملاحظات:**
- أضف أي معلومة مفيدة:
  - حالة العائلة
  - ملاحظات على المنزل
  - أفضل وقت للزيارة
  - رقم هاتف إضافي
  - أي شيء يساعد في المستقبل

---

## 🚀 رفع المشروع للإنتاج

### ما المقصود بـ "الإنتاج" (Production)؟

**Development (التطوير):**
- تعمل على جهازك المحلي
- http://localhost:5173
- للتجربة والتطوير فقط

**Production (الإنتاج):**
- متاح على الإنترنت للجميع
- https://your-domain.com
- سريع ومُحسّن
- متاح 24/7

### الخطوات الأساسية

#### 1. البناء (Build)

**ما هو البناء؟**
- تحويل كود React + TypeScript إلى JavaScript عادي
- تصغير الملفات (minification)
- تحسين الأداء
- دمج جميع الملفات

**كيف؟**
```bash
npm run build
```

**النتيجة:**
- مجلد جديد اسمه `dist`
- يحتوي على ملفات HTML, CSS, JS جاهزة
- حجم أصغر بكثير
- سرعة أعلى

**محتويات مجلد dist:**
```
dist/
├── index.html
├── assets/
│   ├── index-xxx.css    # ملف CSS واحد مدمج
│   └── index-xxx.js     # ملف JS واحد مدمج
└── ...
```

#### 2. اختبار البناء محلياً

```bash
npm run preview
```

- يفتح على http://localhost:4173
- تأكد أن كل شيء يعمل
- إذا وجدت مشكلة، اصلحها وأعد البناء

#### 3. اختيار خدمة الاستضافة

**ما هي الاستضافة؟**
- سيرفر على الإنترنت يعرض موقعك
- متاح للجميع 24/7
- يوفر HTTPS ونطاق

**خيارات الاستضافة:**
- Vercel (موصى به)
- Netlify
- VPS
- استضافة عربية

### الخيار 1: Vercel (الأسهل والأفضل)

#### لماذا Vercel؟

✅ **مجاني بالكامل**
✅ **سريع جداً (CDN عالمي)**
✅ **SSL مجاني**
✅ **نشر تلقائي من Git**
✅ **سهل للمبتدئين**

#### خطوات النشر على Vercel:

**أولاً: رفع الكود على GitHub**

1. **أنشئ حساب GitHub** (إن لم يكن لديك):
   - اذهب إلى https://github.com
   - اضغط "Sign up"

2. **أنشئ repository جديد:**
   - اضغط "New repository"
   - اسم: `zakat-system` (أو أي اسم)
   - اختر: Public أو Private
   - اضغط "Create repository"

3. **ارفع الكود:**
   ```bash
   # في مجلد المشروع
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/username/repo-name.git
   git push -u origin main
   ```

**ثانياً: النشر على Vercel**

1. **اذهب إلى https://vercel.com**
2. **اضغط "Sign Up"**
3. **سجل دخول باستخدام GitHub**
4. **اضغط "New Project"**
5. **اختر repository الذي أنشأته**
6. **Vercel سيكتشف إعدادات Vite تلقائياً**

7. **أضف Environment Variables:**
   - في صفحة الإعدادات، اضغط "Environment Variables"
   - أضف:
     ```
     Name: VITE_SUPABASE_URL
     Value: your-supabase-url

     Name: VITE_SUPABASE_ANON_KEY
     Value: your-anon-key
     ```
   - **مهم جداً:** لا تنسى هذه الخطوة!

8. **اضغط "Deploy"**
9. **انتظر 1-2 دقيقة**
10. **✅ موقعك جاهز!**

**سيعطيك Vercel رابط مثل:**
```
https://zakat-system.vercel.app
```

**التحديثات المستقبلية:**
- فقط افعل `git push`
- Vercel سينشر تلقائياً!

### الخيار 2: Netlify

#### خطوات النشر على Netlify:

**الطريقة 1: من GitHub (مثل Vercel)**
1. اذهب إلى https://netlify.com
2. سجل دخول
3. "Add new site" > "Import an existing project"
4. اربط GitHub
5. اختر repository
6. أضف Environment Variables
7. اضغط "Deploy"

**الطريقة 2: Drag & Drop (سهلة جداً)**
1. ابني المشروع: `npm run build`
2. اذهب إلى https://netlify.com
3. اسحب مجلد `dist` وأفلته على الصفحة
4. ✅ موقعك منشور!

**⚠️ ملاحظة:**
- الطريقة 2 (Drag & Drop) لن تحدّث تلقائياً
- ستحتاج لرفع مجلد `dist` يدوياً كل مرة

**إضافة Environment Variables في Netlify:**
1. Site settings
2. Build & deploy
3. Environment
4. أضف المتغيرات

### الخيار 3: VPS (للمحترفين)

**ما هو VPS؟**
- Virtual Private Server
- سيرفر كامل تتحكم فيه
- أكثر مرونة لكن أصعب

**الخدمات المتاحة:**
- DigitalOcean ($5/شهر)
- Linode
- Vultr
- AWS EC2
- Google Cloud

**خطوات النشر على VPS:**

1. **إنشاء Server:**
   - اختر Ubuntu 22.04 LTS
   - احصل على IP address

2. **الاتصال:**
   ```bash
   ssh root@your-server-ip
   ```

3. **تثبيت Node.js:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

4. **تثبيت Nginx:**
   ```bash
   sudo apt update
   sudo apt install nginx
   ```

5. **رفع الكود:**
   ```bash
   # على جهازك
   npm run build
   scp -r dist/* root@your-server-ip:/var/www/html/
   ```

6. **إعداد Nginx:**
   ```bash
   sudo nano /etc/nginx/sites-available/default
   ```

   أضف:
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       root /var/www/html;
       index index.html;

       location / {
           try_files $uri $uri/ /index.html;
       }
   }
   ```

7. **إعادة تشغيل:**
   ```bash
   sudo systemctl restart nginx
   ```

8. **SSL مجاني:**
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d your-domain.com
   ```

### شراء نطاق (Domain Name)

**ما هو النطاق؟**
- عنوان موقعك (مثل: zakat-amerat.com)
- بدلاً من رابط Vercel الطويل

**أين تشتري؟**
- Namecheap: https://namecheap.com
- GoDaddy: https://godaddy.com
- Google Domains
- مواقع عربية

**السعر:**
- .com: $10-15 سنوياً
- .org: $10-15 سنوياً
- .net: $10-15 سنوياً

**ربط النطاق بـ Vercel:**
1. في Vercel، اذهب إلى "Domains"
2. أضف نطاقك
3. Vercel سيعطيك DNS records
4. أضفها في موقع النطاق
5. انتظر 24 ساعة للتفعيل

### ملاحظات مهمة للإنتاج

#### 1. Environment Variables

**في Production، لا يوجد ملف .env!**

بدلاً من ذلك:
- في Vercel: أضف المتغيرات في الإعدادات
- في Netlify: نفس الشيء
- في VPS: أنشئ ملف .env على السيرفر

#### 2. قاعدة البيانات

**لا تنقل قاعدة البيانات!**
- قاعدة البيانات موجودة على Supabase
- فقط تأكد من:
  - المتغيرات صحيحة
  - مشروع Supabase مفعّل

#### 3. الأمان

**تحقق:**
- ✅ ملف .env غير مرفوع على Git
- ✅ متغيرات البيئة صحيحة
- ✅ HTTPS مفعّل
- ✅ Supabase RLS مفعّلة

#### 4. الأداء

**نصائح:**
- استخدم CDN (Vercel/Netlify يوفرونه)
- فعّل الـ caching
- صغّر الصور
- استخدم lazy loading

---

## 🔧 حل المشاكل الشائعة

### 1. خطأ في الاتصال بـ Supabase

**الأعراض:**
```
Failed to fetch
Invalid API key
Network error
```

**الحلول:**

**أ) تحقق من ملف .env:**
```bash
# تأكد أنه موجود
ls -la .env

# تأكد من المحتوى
cat .env
```

يجب أن يحتوي على:
```env
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc...
```

**ب) تأكد من القيم صحيحة:**
- اذهب إلى Supabase Dashboard > Settings > API
- قارن القيم

**ج) أعد تشغيل dev server:**
```bash
# أوقف السيرفر (Ctrl+C)
npm run dev
```

### 2. لا يمكن تسجيل الدخول

**السبب المحتمل 1: المستخدم غير موجود**

**الحل:**
1. اذهب إلى Supabase Dashboard
2. Authentication > Users
3. تأكد من وجود المستخدم
4. إذا لم يكن موجوداً، أضفه

**السبب المحتمل 2: المستخدم في auth.users لكن ليس في profiles**

**الحل:**
```sql
-- في SQL Editor
INSERT INTO profiles (id, full_name, role, phone, is_active)
VALUES (
  'USER_ID_FROM_AUTH',
  'اسم المستخدم',
  'admin',
  '99999999',
  true
);
```

**السبب المحتمل 3: الحساب معطّل**

**الحل:**
```sql
UPDATE profiles
SET is_active = true
WHERE id = 'USER_ID';
```

### 3. خطأ RLS Policy

**الأعراض:**
```
new row violates row-level security policy
permission denied for table xxx
```

**السبب:**
- RLS policies غير صحيحة
- أو لم يتم تطبيق migrations

**الحل:**

1. **تأكد من تطبيق migrations:**
   - اذهب إلى SQL Editor
   - نفذ ملف `create_complete_schema.sql`

2. **أعد إنشاء policy معينة:**
   ```sql
   -- مثال لإعادة إنشاء policy
   DROP POLICY IF EXISTS "Users can read profiles" ON profiles;

   CREATE POLICY "Users can read profiles"
     ON profiles FOR SELECT
     TO authenticated
     USING (true);
   ```

### 4. الصفحة بيضاء بعد تسجيل الدخول

**السبب:**
- خطأ JavaScript
- بيانات المستخدم غير كاملة

**الحل:**

1. **افتح Developer Console (F12)**
2. **اذهب إلى Console tab**
3. **شاهد الأخطاء**
4. **غالباً:**
   - `user.profile is null` → المستخدم غير موجود في profiles
   - `Cannot read property` → بيانات ناقصة

5. **تحقق من profiles:**
   ```sql
   SELECT * FROM profiles WHERE id = 'USER_ID';
   ```

### 5. Edge Function لا تعمل

**السبب:**
- لم يتم نشر Edge Function

**الحل:**

**إذا كان لديك CLI:**
```bash
supabase functions deploy create-user
```

**إذا لم يكن لديك CLI:**
- أضف المستخدمين يدوياً من Dashboard
- راجع "إنشاء أول مستخدم" أعلاه

### 6. خطأ في البناء (npm run build)

**الأعراض:**
```
Error: Failed to build
Type error: ...
Module not found: ...
```

**الحل:**

1. **احذف node_modules:**
   ```bash
   rm -rf node_modules package-lock.json
   ```

2. **أعد التثبيت:**
   ```bash
   npm install
   ```

3. **حاول البناء مرة أخرى:**
   ```bash
   npm run build
   ```

4. **إذا استمر الخطأ:**
   - اقرأ رسالة الخطأ بعناية
   - ابحث عن الخطأ في Google
   - تحقق من أن جميع الملفات موجودة

### 7. البيانات لا تظهر

**السبب:**
- الجداول فارغة

**الحل:**
- استخدم `sample-data.sql` لإضافة بيانات تجريبية
- أو أضف بيانات يدوياً من Table Editor

### 8. الموقع بطيء

**الحلول:**

1. **استخدم CDN (Vercel/Netlify)**
2. **تحقق من استعلامات قاعدة البيانات:**
   - هل تجلب بيانات كثيرة؟
   - استخدم pagination

3. **صغّر الصور:**
   - استخدم صيغ حديثة (WebP)
   - قلّل الدقة

4. **فعّل lazy loading**

---

## 🔐 ملاحظات أمنية مهمة جداً

### ما يجب حمايته

#### 1. ملف .env

**⚠️ خطر كبير!**

هذا الملف يحتوي على معلومات حساسة:
```env
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...
```

**احمه:**
- ✅ لا ترفعه على GitHub
- ✅ لا تشاركه مع أحد
- ✅ تأكد أنه في `.gitignore`

**ملف .gitignore يجب أن يحتوي:**
```gitignore
.env
.env.local
.env.production
.env.*.local
```

#### 2. Database Password

عند إنشاء مشروع Supabase، تختار كلمة مرور:
- **احفظها في مكان آمن**
- **لا تشاركها**
- **استخدم كلمة قوية**

#### 3. service_role key

في Supabase Dashboard > API:
- ستجد `service_role key`
- **لا تستخدمها في Frontend أبداً!**
- **فقط للاستخدام في Backend**

#### 4. كلمات مرور المستخدمين

**نصائح:**
- استخدم كلمات مرور قوية
- لا تكتبها في الكود
- لا تشاركها
- غيّرها دورياً

### أفضل الممارسات الأمنية

#### 1. استخدم HTTPS فقط
- Vercel/Netlify يوفرونه تلقائياً
- VPS: استخدم Let's Encrypt

#### 2. RLS مفعّلة
تأكد أن جميع الجداول محمية بـ RLS:
```sql
ALTER TABLE table_name ENABLE ROW LEVEL SECURITY;
```

#### 3. Audit Log
- راجع operation_logs دورياً
- ابحث عن أنشطة مشبوهة

#### 4. النسخ الاحتياطي
- Supabase يعمل نسخ احتياطي تلقائي
- لكن يمكنك عمل نسخ إضافية:
  - SQL Editor > Export

---

## 📞 الدعم والمساعدة

### إذا واجهت مشكلة

**الخطوات:**

1. **راجع هذا الملف أولاً**
   - معظم المشاكل مشروحة هنا

2. **تحقق من Console**
   - اضغط F12
   - اذهب إلى Console
   - اقرأ رسالة الخطأ

3. **راجع Supabase Logs**
   - Dashboard > Logs
   - شاهد أخطاء قاعدة البيانات

4. **ابحث في Google**
   - انسخ رسالة الخطأ
   - ابحث عنها
   - غالباً ستجد الحل

5. **تواصل مع المطور**
   - أرسل:
     - وصف المشكلة
     - رسالة الخطأ
     - خطوات إعادة المشكلة
     - screenshots

### موارد مفيدة

#### التوثيق الرسمي:
- **React**: https://react.dev/
- **TypeScript**: https://www.typescriptlang.org/docs
- **Supabase**: https://supabase.com/docs
- **Tailwind CSS**: https://tailwindcss.com/docs
- **Vite**: https://vitejs.dev/guide

#### دورات عربية مجانية:
- أساسيات React
- TypeScript للمبتدئين
- Supabase من الصفر
- Tailwind CSS بالعربي

#### مجتمعات عربية:
- مجتمعات Discord للمطورين العرب
- مجموعات Facebook
- منتديات البرمجة

---

## 🎓 نصائح للتطوير والتعديل

### إذا أردت التعديل على الكود

#### 1. ابدأ صغيراً
- لا تغيّر كل شيء مرة واحدة
- غيّر شيء واحد، اختبره، ثم انتقل للتالي

#### 2. استخدم Git
```bash
# احفظ التغييرات
git add .
git commit -m "وصف التغيير"

# إذا حدثت مشكلة، تراجع
git revert HEAD
```

#### 3. اختبر محلياً أولاً
- لا تنشر مباشرة للإنتاج
- اختبر على localhost:5173

#### 4. اقرأ الكود
- حاول فهم كيف يعمل قبل التعديل
- اقرأ التعليقات

#### 5. احفظ نسخة احتياطية
- قبل تغيير كبير، احفظ نسخة

---

## 📄 الترخيص

هذا المشروع خاص بـ **لجنة زكاة العامرات**.

جميع الحقوق محفوظة © 2024

---

## 🙏 الخاتمة

تم تطوير هذا النظام بـ ❤️ لخدمة الأسر المستفيدة.

نسأل الله أن ينفع به ويجعله في ميزان حسنات كل من ساهم فيه.

**لجنة زكاة العامرات**

---

**آخر تحديث:** ديسمبر 2024
**الإصدار:** 1.0.0
